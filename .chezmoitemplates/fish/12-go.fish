# =============================================================================
# 12 — Go
# =============================================================================
# GOPATH is ~/.go (set by install script instead of default ~/go)
fish_add_path $HOME/.go/bin
