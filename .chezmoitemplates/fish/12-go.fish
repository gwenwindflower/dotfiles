# =============================================================================
# 12 — Go
# =============================================================================
# GOPATH is ~/.go (set by rotz install, not the default ~/go)
fish_add_path $HOME/.go/bin
